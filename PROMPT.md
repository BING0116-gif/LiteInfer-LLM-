# 开发窗口标准 Prompt

> 用法：每开一个新对话窗口（建议每个 Task 一个），把下面的内容复制进去，
> 把【Task XX】换成当前要做的任务编号即可。规范以仓库内 07 号文档为准，不需要改动本 prompt。

---

你现在是 LiteInfer 项目的主程。

## 第一步（必做，不读完不许动代码）

完整阅读以下文件，它是本项目唯一开发规范，优先级高于我下面的任何指令：

- `docs/07_Agent_Development_Instructions.md`（开发规范，含补充条款 A）

再读这两份了解全局（了解即可，禁止提前实现）：

- `docs/00_README.md`
- `docs/02_Technical_Architecture.md`

## 运行环境（硬性，违反即返工）

- 我的开发机没有 NVIDIA GPU，只有 CPU。当前阶段必须能在 CPU 上完整跑通。
- 禁止出现裸 `"cuda"` 字面量（`"cuda"` / `.cuda()` / `.to("cuda")`），设备统一走 `EngineConfig.device`
- dtype 走 `EngineConfig.dtype`，CPU 用 `torch.float32`（CPU 上 FP16 又慢又不准）
- `torch.cuda.*` 调用必须有 CPU fallback，无 GPU 时不得抛异常
- 项目根目录即当前仓库根目录
- 模型缓存通过 `HF_HOME` 指向 `D:\LiteInfer\hf_cache`，禁止下载到 C 盘

## 本次任务

只执行【Task 01：项目骨架 + HF Baseline】。
禁止提前实现 Task 02 及之后的任何功能。

## 开始前先输出（此阶段不要写代码）

A. 本阶段目标
B. 需要掌握的知识
C. 本阶段架构
D. 将创建/修改的文件（完整路径）
E. 验收标准（必须能在 CPU 上执行）

我确认后再开始编码。

## 编码要求

1. 一次只做一个 Task，代码量控制在能完整验证的范围内
2. 每个测试必须真实运行，把终端原始输出贴给我；禁止编写"看起来对"的测试然后宣称通过
3. 报错时先贴完整 traceback，禁止猜测式修改
4. 类型标注 + 关键注释；注释解释"为什么这么做"，不要复述代码
5. 发现方案有技术冲突：停下来，指出问题、解释原因、给出修改建议，禁止擅自修改方案

## 完成后交付

按 07 号文档第三节的 9 项交付，外加：

- 测试的真实终端输出
- 本次新增/修改文件清单

## 版本控制

不要执行 `git commit` / `git push`，全部交给我。
